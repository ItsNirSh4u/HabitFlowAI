package com.habitflow.app
}
    }
        )
            database = database
            apiService = ApiClient.apiService,
        repository = HabitFlowRepository(
        // Initialize Repository

        ).build()
            "habitflow_database"
            HabitFlowDatabase::class.java,
            applicationContext,
        database = Room.databaseBuilder(
        // Initialize Room Database

        super.onCreate()
    override fun onCreate() {

        private set
    lateinit var repository: HabitFlowRepository

        private set
    lateinit var database: HabitFlowDatabase

class HabitFlowApplication : Application() {
 */
 * HabitFlow Application Class
/**

import com.habitflow.app.data.repository.HabitFlowRepository
import com.habitflow.app.data.local.HabitFlowDatabase
import com.habitflow.app.data.api.ApiClient
import androidx.room.Room
import android.app.Application


